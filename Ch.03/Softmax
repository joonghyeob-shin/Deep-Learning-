# exp함수로 인한 overflow를 막고자 함수식에 약간의 꼼수를 부림.

def softman(a):
  C = np.max(a)               
  exp_a = np.exp(a - c)         # 배열에서 인수에 배열의 최대값을 빼줌으로써 exp함수에 의한 overflow를 막는다.
  sum_exp_a = np.sum(exp_a)
  
  return y
